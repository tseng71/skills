interface:
  display_name: 'image-deck: GPT Image 2 Slide Deck'
  short_description: image-deck requires overall-design approval, then separate
    sample-style approval before generating remaining slides.
  default_prompt: Use image-deck to create a consistent GPT Image 2 slide deck / PPT
    / PowerPoint presentation. After overall-design approval, generate exactly one
    master sample, show it and stop, then continue with the remaining slides only
    after I separately approve the displayed sample's style.
